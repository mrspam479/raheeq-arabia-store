# Force LF line endings for all text files
* text=auto eol=lf

# Explicitly ensure shell scripts always use LF
*.sh text eol=lf

# Windows-specific files can keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
